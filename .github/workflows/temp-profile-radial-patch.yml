name: Temporary profile radial patch

on:
  push:
    branches: [experiment/profile-radial-account-switcher]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'chore: activate profile radial patch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: experiment/profile-radial-account-switcher
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/scripts/profile_radial_patch.py
      - name: Validate diff
        run: git diff --check
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib test
          git commit -m 'feat: restore adaptive profile radial switcher'
          git push origin HEAD:experiment/profile-radial-account-switcher
