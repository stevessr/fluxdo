name: Refine preload pipeline

on:
  push:
    branches:
      - perf/preload-pipeline-progress

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: perf/preload-pipeline-progress
          fetch-depth: 0
      - name: Refine preload patch
        run: python3 .github/scripts/refine_preload_patch.py
      - name: Validate patch
        run: git diff --check
      - name: Commit refinement
        shell: bash
        run: |
          rm .github/scripts/refine_preload_patch.py
          rm .github/workflows/refine-preload-pipeline.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "perf(preload): preserve first-batch latency"
          git push origin HEAD:perf/preload-pipeline-progress
