name: Apply preload pipeline optimization

on:
  push:
    branches:
      - perf/preload-pipeline-progress

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: perf/preload-pipeline-progress
          fetch-depth: 0
      - name: Apply targeted preload patch
        run: python3 .github/scripts/apply_preload_patch.py
      - name: Validate patch
        run: git diff --check
      - name: Commit patch
        shell: bash
        run: |
          rm .github/scripts/apply_preload_patch.py
          rm .github/workflows/apply-preload-pipeline-progress.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "perf(preload): overlap decode and parallelize topic batches"
          git push origin HEAD:perf/preload-pipeline-progress
