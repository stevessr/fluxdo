name: Apply reply composer patch

on:
  push:
    branches: ["feat/discourse-reply-composer"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/discourse-reply-composer
          fetch-depth: 0

      - name: Apply scoped composer changes
        run: python3 .github/scripts/apply_reply_composer_patch.py

      - name: Validate and commit
        shell: bash
        run: |
          rm .github/workflows/apply-reply-composer-patch.yml
          rm .github/scripts/apply_reply_composer_patch.py
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(composer): add Discourse-style action switching"
          git push origin HEAD:feat/discourse-reply-composer
