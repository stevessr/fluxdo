name: Validate category drawer patch runner

on:
  push:
    branches: [feat/category-drawer-more]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo runner-ok
